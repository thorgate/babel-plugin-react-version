{
  "name": "babel-plugin-react-version",
  "version": "0.1.0",
  "author": "Thorgate <info@thorgate.eu>",
  "description": "Babel plugin to build multi-version react components",
  "devDependencies": {
    "codecov.io": "^0.1.2",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.13",
    "mocha": "^2.2.4",
    "should": "^7.0.1"
  },
  "scripts": {
    "test": "mocha",
    "travis": "npm run cover -- --report lcovonly",
    "cover": "istanbul cover node_modules/mocha/bin/_mocha",
    "publish-patch": "mocha && npm version patch && git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:thorgate/babel-plugin-react-version.git"
  },
  "license": "MIT"
}
